require 'test_helper'

class Judge::ActiveRecordTest < ActiveSupport::TestCase
  context 'can?' do
    should_eventually 'support calls to athorized? on class level' do
    end
    should_eventually 'support calls to athorized? on instance' do
    end
    
  end
end